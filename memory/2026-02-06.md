# 2026-02-06 - Alek Project Manager Setup

## Ważne lekcje

### Codex zawsze z --yolo
**Zasada:** ZAWSZE używaj `codex --yolo exec` lub `--dangerously-bypass-approvals-and-sandbox`
**Powód:** Read-only sandbox blokuje edycje plików, approvals nic nie dają
**Zastosowanie:** Codex do WSZYSTKICH zadań developerskich, Alek NIGDY nie koduje bezpośrednio
**UWAGA:** NIE używać `codex-auth` - psuje Codexa! Zalogować ręcznie, używać `codex` bezpośrednio.

### Workflow projektu msds-portal
**REPO:** `https://github.com/mikolaj92/msds-portal`
**LOKALNIE:** `/Users/mini-m4-1/Developer/msds-portal/` (monorepo)
**BRANCH:** `develop` (główny branch, BRAK PRów!)
**PRODUCTION:** `http://localhost:3000` (0.0.0.0:3000 - to TO SAMO!)

**Proces PRZED commitem:**
1. **CRITICAL:** Health check: `curl -s http://localhost:3000/api/health`
2. Sprawdź czy serwer odpowiada (jeżeli NIE → safe to deploy)
3. Jeżeli server STOI i odpowiada → zastanów się 2x czy commitować

**Proces:**
1. Brak Pull Requests - pushujemy wszystko na `develop`
2. Po pushu: ZAWSZE restart wszystkich launchd agents
3. Commit message: `#issue_number` - GitHub automatycznie łączy z issue
4. **PRZED commitem:** Health check na `localhost:3000/api/health`

**Codex workflow:**
1. Planowanie → proponuje rozwiązania
2. Jeżeli 1 rozwiązanie → od razu wykonuje
3. Jeżeli >1 rozwiązań → konsultuje ze mną, wybieram najbardziej sensowne, wracam z decyzją
4. Po implementacji → commit z `#issue_number` → push na develop
5. **CRITICAL:** Przed push - health check na localhost:3000
6. **CRITICAL:** Codex robi restart launchd agents po pushu!

### Struktura Rudy workers
- msds-portal/control-plane/ - dashboard (port 3000)
- msds-portal/rudy-worker/ - worker 1 (ma db/)
- msds-portal/rudy-worker-mlx/ - worker 2 (ma db/)

### Ścieżki do bazy w workers
**Stara ścieżka:** `repo_root.parent / "rudy" / "db" / "substances_regulation.db`
**Nowa ścieżka:** `repo_root / "db" / "substances_regulation.db"`

**Pliki do aktualizacji:**
- rudy-worker/src/rudy_worker/main.py:42
- rudy-worker/src/rudy_worker/main_fastapi.py:75
- rudy-worker-mlx/src/rudy_worker/main.py:175

### Launchd Agents (wszystkie w msds-portal)
**Ścieżka do .plist:** `~/Library/LaunchAgents/`
- ai.control-plane.plist → msds-portal/control-plane
- ai.rudy-worker.plist → msds-portal/rudy-worker
- ai.rudy-worker-mlx.plist → msds-portal/rudy-worker-mlx

**Log paths:**
- msds-portal/control-plane/control-plane.launchd.*.log
- msds-portal/rudy-worker/rudy-worker.launchd.*.log
- msds-portal/rudy-worker-mlx/rudy-worker-mlx.launchd.*.log

**RESTART PO PUSU:**
```bash
launchctl unload ~/Library/LaunchAgents/ai.control-plane.plist
launchctl unload ~/Library/LaunchAgents/ai.rudy-worker.plist
launchctl unload ~/Library/LaunchAgents/ai.rudy-worker-mlx.plist
launchctl load ~/Library/LaunchAgents/ai.control-plane.plist
launchctl load ~/Library/LaunchAgents/ai.rudy-worker.plist
launchctl load ~/Library/LaunchAgents/ai.rudy-worker-mlx.plist
```

### Health Check PRZED commitem
**Command:** `curl -s http://localhost:3000/api/health`
**Cel:** Upewnić się że production działa przed wdrożeniem zmian
**Zastosowanie:** ZAWSZE przed `git commit`
**UWAGA:** `localhost:3000` == PRODUCTION (serwer na 0.0.0.0:3000)

### GitHub CLI
**Zalogowany jako:** mikolaj92
**Repo:** https://github.com/mikolaj92/msds-portal
**Dostępne komendy:**
- gh issue list / gh issue view <number>
- gh pr list / gh pr view <number>
- gh pr checks <number> / gh pr merge <number>

### Codex auth
**UWAGA:** NIE używać `codex-auth` - psuje Codexa!
**Logowanie:** Manualne przez interfejs Codexa
**Użycie:** `codex --yolo exec "task"` (zawsze z --yolo)

## Completed Tasks
1. ✅ Reorganizacja workspace z 4 do 3 folderów (usunięto rudy/)
2. ✅ Przeniesienie bazy danych do każdego workera
3. ✅ Aktualizacja ścieżek w kodzie
4. ✅ Odzyskanie git repo z msds-portal po destrukcyjnych zmianach
5. ✅ Reset launchd agents do msds-portal
6. ✅ Issue #1: "Lepszy UX podczas masowego uploadu PDF" (commit 877e617)
   - Draft project + progress upload
   - Retry błędów
   - Blokada przycisku "Stwórz projekt"
   - Auto-close issue przez "Fix #1:" w commit message

## Aktualne zadanie
**Brak aktywnych issues** - Issue #1 zamknięty przez auto-close

## QMD context
- Używać QMD jako pamięci (ogranicza zużycie tokenów)
- Indeksować: `qmd update`
- Szukać: `qmd search "query"` (BM25 - domyślne)
- Semantic: `qmd vsearch "query"` (wolniejsze, tylko gdy potrzebne)
