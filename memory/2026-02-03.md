# 2026-02-03

## Control-plane / rudy-worker

- TODO (worker): przy resume/requeue/restart domykać "otwarte" spany PDF (brak span_end) jako `span_end status=failed` z `error=interrupted` (albo podobnie), żeby nie zostawały wiszące nody. Obecnie dashboard oznacza je jako `stale`, ale lepiej naprawić źródło.
- Notatka: zdarzył się błąd "address already in use" przy restarcie uvicorn na porcie 3000 (zbyt szybki restart / drugi proces). Warto zrobić docelowo launchd dla control-plane lub bezpieczny restart.
- Patryk: wszystkie projekty dev trzyma w `~/Developer`.
